import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'
import './custom-sonner-progress.css' // Import custom Sonner progress bar styles

createRoot(document.getElementById("root")!).render(<App />);
